# Stats object
type Stats {
  completePercentage: Int
  newKeysCount: Int
  missingTranslationsCount: Int
  translationKeysCount: Int
  localesCount: Int
  projectsCount: Int
}

extend type Query {
  # Get global stats (stats on all projects)
  getGlobalStats: Stats
}
