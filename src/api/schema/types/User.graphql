type User {
  id: ID
  createdAt: Date
  updatedAt: Date
  isValidated: Boolean
  email: String
  name: String
}

type CreateUserResponse {
  status: Status!
  errors: [ValidationError]!
  user: User
}

extend type Mutation {
  createUser(
    email: String!
    name: String!
    passwordPlain: String!
  ): CreateUserResponse
}
