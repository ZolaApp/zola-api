# Locale object
type Locale {
  id: ID
  name: String
  code: String
  missingTranslations: Int
  completePercentage: Int
}

extend type Query {
  # Get all available locales
  getLocales: [Locale]
}
